# Source this file before running configure-openclaw-otel or print-otlp-env
# These values are intentionally placeholders and must be replaced per node.

export FLEET_INGEST_TOKEN="REPLACE_WITH_NODE_INGEST_TOKEN"

# Node label, endpoint, network mode, service name, and OpenClaw config path are
# read from the node-local JSON file passed with --config, or from explicit CLI
# flags such as --node-label and --endpoint.

# Optional off-LAN Cloudflare Access headers. Leave unset for LAN nodes.
# export CF_ACCESS_CLIENT_ID="REPLACE_WITH_CF_ACCESS_CLIENT_ID"
# export CF_ACCESS_CLIENT_SECRET="REPLACE_WITH_CF_ACCESS_CLIENT_SECRET"
