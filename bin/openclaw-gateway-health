#!/usr/bin/env sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
ROOT_DIR="${SCRIPT_DIR}/.."

exec sh "$ROOT_DIR/src/fleet_node_observability/collectors/openclaw_gateway_health.sh" "$@"
