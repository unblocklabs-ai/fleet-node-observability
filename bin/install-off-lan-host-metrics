#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
ROOT_DIR="${SCRIPT_DIR}/.."

exec "$ROOT_DIR/src/fleet_node_observability/installers/off_lan_host_metrics.sh" "$@"
