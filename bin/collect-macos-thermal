#!/usr/bin/env sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PYTHON_BIN="${CODEX_NODE_PYTHON:-python3}"
ROOT_DIR="${SCRIPT_DIR}/.."

exec "$PYTHON_BIN" "$ROOT_DIR/src/fleet_node_observability/commands/collect_macos_thermal.py" "$@"
